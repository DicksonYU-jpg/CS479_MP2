# CS479_Learning_in_NN

# Initialization
- Download Conda is not installed (miniconda is recommended)
- Create conda environment and install dependencies: `bash setup_conda_env.sh` (Under Bash, not PowerShell)
- Activate the environment: `conda activate cs479-mp2`